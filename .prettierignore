# Ignore artifacts:
build
coverage

# Generated by scripts/handbook/. Written minified because graph.json is
# fetched on every page load; formatting it costs ~640KB and churns the whole
# file on every re-scrape, since prettier collapses short arrays and
# JSON.stringify does not.
public/uwa-units
